$schema: 'http://json-schema.org/draft-07/schema#'
type: array
items:
  type: object
  properties:
    when:
      type: string
      format: whenzel
    message:
      type: string
  required: [when, message]
